// a simple function that adds two numbers
let addTwoNumbers = func(x, y) {
    return x + y
}

// how about three
let addThreeNumbers = func(x, y, z) {
    return x + y + z
}

/*
*
* Every donut app has to have a main entrypoint
*/
let main = func() {
    let a = 22
    let b = 33
    return addTwoNumbers(a, b)
}

addTwoNumbers(777, 888)
